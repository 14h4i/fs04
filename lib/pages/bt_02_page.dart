import 'package:flutter/material.dart';

class Bt02Page extends StatelessWidget {
  const Bt02Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BT 02'),
      ),
      body: Container(),
    );
  }
}
